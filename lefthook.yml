pre-commit:
    parallel: true
    commands:
        biome:
            glob: '*.{js,ts,svelte,json,jsonc,css,html}'
            run: bunx biome check --write --no-errors-on-unmatched {staged_files}
            stage_fixed: true

pre-push:
    commands:
        check:
            run: bun run check
