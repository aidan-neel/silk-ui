export declare const button: import('tailwind-variants').TVReturnType<
    {
        variant: {
            /**
             * `data-[state=open]` mirrors the hover fill on every variant so a
             * popover or menu trigger reads as hovered for as long as its surface
             * is open.
             */
            primary: string;
            secondary: string;
            ghost: string;
            quiet: string;
            /**
             * Outline keeps the soft raised shadow. Focus composes the ring on top
             * of the lift so the raised look survives focus.
             */
            outline: string;
            destructive: string;
            /**
             * A clickable Panel: the same interaction as `outline` wearing Panel's
             * concentric frame -- the semantic border outside and the inset surface
             * ring inside read as Panel's double edge.
             */
            panel: string;
        };
        size: {
            sm: string;
            md: string;
            lg: string;
            icon: string;
        };
    },
    undefined,
    'sivir-press inline-flex h-[calc(var(--size-control-md)-2px)] hover:cursor-[var(--ui-cursor-interactive)] items-center justify-center gap-[var(--sivir-space-2)] whitespace-nowrap select-none rounded-[var(--radius-lg)] px-[calc(var(--sivir-space-3)+2px)] [font-size:var(--font-size-button)] [font-weight:var(--font-weight-button)] [letter-spacing:var(--tracking-button)] leading-none antialiased transition-[background-color,border-color,color,box-shadow,transform,scale] [transition-duration:var(--motion-duration-press)] ease-[var(--ease-press)] motion-reduce:transition-none focus-visible:outline-none focus-visible:ring-0 focus-visible:shadow-[var(--focus-ring)] disabled:pointer-events-none disabled:opacity-40 aria-disabled:cursor-default [&_svg]:pointer-events-none [&_svg:not([class*="size-"])]:size-4 [&_svg]:shrink-0',
    {
        variant: {
            /**
             * `data-[state=open]` mirrors the hover fill on every variant so a
             * popover or menu trigger reads as hovered for as long as its surface
             * is open.
             */
            primary: string;
            secondary: string;
            ghost: string;
            quiet: string;
            /**
             * Outline keeps the soft raised shadow. Focus composes the ring on top
             * of the lift so the raised look survives focus.
             */
            outline: string;
            destructive: string;
            /**
             * A clickable Panel: the same interaction as `outline` wearing Panel's
             * concentric frame -- the semantic border outside and the inset surface
             * ring inside read as Panel's double edge.
             */
            panel: string;
        };
        size: {
            sm: string;
            md: string;
            lg: string;
            icon: string;
        };
    },
    undefined,
    import('tailwind-variants').TVReturnType<
        {
            variant: {
                /**
                 * `data-[state=open]` mirrors the hover fill on every variant so a
                 * popover or menu trigger reads as hovered for as long as its surface
                 * is open.
                 */
                primary: string;
                secondary: string;
                ghost: string;
                quiet: string;
                /**
                 * Outline keeps the soft raised shadow. Focus composes the ring on top
                 * of the lift so the raised look survives focus.
                 */
                outline: string;
                destructive: string;
                /**
                 * A clickable Panel: the same interaction as `outline` wearing Panel's
                 * concentric frame -- the semantic border outside and the inset surface
                 * ring inside read as Panel's double edge.
                 */
                panel: string;
            };
            size: {
                sm: string;
                md: string;
                lg: string;
                icon: string;
            };
        },
        undefined,
        'sivir-press inline-flex h-[calc(var(--size-control-md)-2px)] hover:cursor-[var(--ui-cursor-interactive)] items-center justify-center gap-[var(--sivir-space-2)] whitespace-nowrap select-none rounded-[var(--radius-lg)] px-[calc(var(--sivir-space-3)+2px)] [font-size:var(--font-size-button)] [font-weight:var(--font-weight-button)] [letter-spacing:var(--tracking-button)] leading-none antialiased transition-[background-color,border-color,color,box-shadow,transform,scale] [transition-duration:var(--motion-duration-press)] ease-[var(--ease-press)] motion-reduce:transition-none focus-visible:outline-none focus-visible:ring-0 focus-visible:shadow-[var(--focus-ring)] disabled:pointer-events-none disabled:opacity-40 aria-disabled:cursor-default [&_svg]:pointer-events-none [&_svg:not([class*="size-"])]:size-4 [&_svg]:shrink-0',
        unknown,
        unknown,
        undefined
    >
>;
