export declare const avatar: import("tailwind-variants").TVReturnType<{
    size: {
        sm: string;
        md: string;
        lg: string;
        xl: string;
    };
    shape: {
        circle: string;
        square: string;
    };
}, undefined, "relative inline-flex shrink-0 select-none items-center justify-center overflow-hidden bg-secondary text-foreground-muted [font-size:var(--font-size-body)] [font-weight:var(--font-weight-body)] [letter-spacing:var(--tracking-body)]", {
    size: {
        sm: string;
        md: string;
        lg: string;
        xl: string;
    };
    shape: {
        circle: string;
        square: string;
    };
}, undefined, import("tailwind-variants").TVReturnType<{
    size: {
        sm: string;
        md: string;
        lg: string;
        xl: string;
    };
    shape: {
        circle: string;
        square: string;
    };
}, undefined, "relative inline-flex shrink-0 select-none items-center justify-center overflow-hidden bg-secondary text-foreground-muted [font-size:var(--font-size-body)] [font-weight:var(--font-weight-body)] [letter-spacing:var(--tracking-body)]", unknown, unknown, undefined>>;
