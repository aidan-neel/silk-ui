/**
 * Manifest schema.
 *
 * Each sivir component exports a `manifest: Manifest` from
 * `components/<name>/manifest.ts`. The CLI consumes these to resolve
 * and install files into consumer projects.
 */
export {};
